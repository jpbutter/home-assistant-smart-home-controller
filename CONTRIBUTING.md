# Contributing

Run **ruff check src tests** and **pytest** before opening a pull request. Use synthetic entity IDs and fixtures. Never commit tokens, exported databases, addresses, camera images or real presence histories. Changes that can switch devices must say so explicitly.